# Garage Buddy — Frontend

This folder contains the Vue 3 + TypeScript frontend for Garage Buddy.

Local development:

```powershell
cd frontend
npm install
npm run dev
```

The dev server proxies `/api` to `http://localhost:8080` (backend).
